import 'package:flutter/material.dart';

class SplashScreenError extends StatelessWidget {
  const SplashScreenError({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text('Error, please try again'),
    );
  }
}
